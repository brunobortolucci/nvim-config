local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "go", "gomod", "gowork", "gosum" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
